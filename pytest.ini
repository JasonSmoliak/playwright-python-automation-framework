[pytest]
testpaths = tests
addopts = -v --tracing=retain-on-failure
pythonpath = .
asyncdemo: demonstration tests for async Playwright learning

markers =
    smoke: critical path tests
    regression: full suite
    ui: UI tests
    api: API tests
    flaky: tests allowed to retry due to external instability
    visual: visual regression tests
